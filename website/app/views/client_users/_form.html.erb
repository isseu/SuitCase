<%= form_for @client_user, :html => { :class => "form-horizontal client_user" } do |f| %>

    <% if @client_user.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@client_user.errors.count, "error") %> prohibited this client_user from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @client_user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :user_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :user_id, :class => 'form-control' %>
    </div>
    <%= error_span(@client_user[:user_id]) %>
  </div>
  <div class="control-group">
    <%= f.label :client_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :client_id, :class => 'form-control' %>
    </div>
    <%= error_span(@client_user[:client_id]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            client_users_path, :class => 'btn btn-default' %>

<% end %>
