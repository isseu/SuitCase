<%= render partial: 'shared/table_generator',
           locals: {
               model_class: User,
               data: @users,
               attributes: [:rut, :name, :first_lastname, :second_lastname, :email]
           }
%>